/**
 * @Scheduled que dispara el muestreo periódico de la instancia configurada
 * (monitor.instancia-id) a un único intervalo (monitor.muestreo.intervalo) --
 * ver PlanificadorMuestreo sobre por qué un solo ciclo combinado y no uno
 * por componente.
 */
package cr.ac.una.monitor.infraestructura.planificador;
