<project>
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>cr.ac.una</groupId>
    <artifactId>monitor-salud-oracle</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>
  <artifactId>monitor-api</artifactId>

  <dependencies>
    <dependency>
      <groupId>cr.ac.una</groupId>
      <artifactId>monitor-infraestructura</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Spring Boot 4 modularizó @WebMvcTest fuera de spring-boot-test-autoconfigure
         (que ahora solo trae json/jdbc): vive en spring-boot-webmvc-test, análogo
         a como spring-boot-starter-web ya trae spring-boot-webmvc en compile. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Verifica físicamente la regla de dependencia hexagonal (ver referencia de arquitectura) -->
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <!-- ADR 0004: artefacto único. Compila monitor-web durante generate-resources,
           antes de que process-resources copie src/main/resources: vite.config.ts
           ya apunta su outDir directo a resources/static, sin paso de copia aparte.
           Así, un "mvn install" desde un clon nuevo produce el jar completo sin
           pasos manuales de npm. node/npm se descargan una sola vez (quedan
           cacheados en monitor-web/node, ver installDirectory); las corridas
           siguientes solo pagan npm install + npm run build. -->
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>1.15.1</version>
        <configuration>
          <workingDirectory>${project.basedir}/../monitor-web</workingDirectory>
          <installDirectory>${project.basedir}/../monitor-web</installDirectory>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <nodeVersion>v22.14.0</nodeVersion>
            </configuration>
          </execution>
          <execution>
            <id>npm-install</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>install</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-build</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
