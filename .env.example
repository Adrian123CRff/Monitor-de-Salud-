# Copia este archivo a .env (que ya está en .gitignore) antes de levantar
# docker-compose. Estas son contraseñas de desarrollo local únicamente —
# nunca las de una instancia real.

ORACLE_PASSWORD=OraclePass123
MONITOR_PASSWORD=MonitorPass123
POSTGRES_USER=monitor
POSTGRES_PASSWORD=HistoricoPass123
